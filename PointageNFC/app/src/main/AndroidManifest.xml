<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.MBDS.pointagenfc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PointageNFC">
        <activity android:name=".ListEtudiantActivity">
        </activity>
        <activity android:name=".SalleLoginActivity">
        </activity>
        <activity android:name=".EnseignantActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EtudiantActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IdentificationActivity" ></activity>

    </application>

</manifest>